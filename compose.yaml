services:
  vue-app:
    build: 
      context: .
      dockerfile: Docker/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # ファイル変更を確実に検知するため